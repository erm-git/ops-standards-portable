# Template Pack

This folder is copied into target repos by `scripts/bootstrap.sh`.

Layout mirrors the target repo:

- `repo-root/` → files that go into the repo root
- `docs/` → files that go into `docs/`
