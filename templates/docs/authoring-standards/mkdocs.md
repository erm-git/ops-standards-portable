---
title: "MkDocs Standards"
status: "standard"
---

## Rules (summary)

- `mkdocs.yml` at repo root (if you publish docs).
- `docs/index.md` is the docs landing page.
- Prefer deterministic nav (e.g., `docs/.nav.yml`) when used.
